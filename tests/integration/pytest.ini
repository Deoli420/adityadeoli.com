[pytest]
asyncio_mode = auto
markers =
    smoke: Critical path tests -- run on every commit
    regression: All CRUD + validation tests -- run on every PR
    integration: Cross-feature flow tests -- run before release
testpaths = .
python_files = test_*.py
python_functions = test_*
